<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Promises and Arrow Functions Example</title>
</head>
<body style="font-family: Arial; padding: 20px;">

  <h2>⚡ JavaScript Promises and Arrow Functions</h2>
  <p id="result" style="font-size:18px; color:darkgreen;"></p>

  <script>
    // Step 1: Create a Promise using an arrow function
    const fetchCatData = () => {
      return new Promise((resolve, reject) => {
        document.getElementById("result").innerHTML = "Fetching cat data... 🐾";

        // Simulate async operation with setTimeout
        setTimeout(() => {
          let success = true;  // change to false to test rejection
          
          if (success) {
            resolve("🐱 Cat data loaded successfully!");
          } else {
            reject("❌ Failed to load cat data!");
          }
        }, 2000);
      });
    };

    // Step 2: Consume the Promise using .then() and .catch()
    fetchCatData()
      .then((message) => {
        document.getElementById("result").innerHTML = message;
      })
      .catch((error) => {
        document.getElementById("result").innerHTML = error;
      });
  </script>

</body>
</html>
